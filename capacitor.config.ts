import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'entrega.v1',
  appName: 'entrega-1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
